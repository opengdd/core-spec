export const RECORD_FORM_COPY = Object.freeze({
  duplicate: "Duplicate",
  editAsText: "Edit as text",
  empty: "Nothing in this record yet",
  addField: "Add a field",
  fieldName: "Field name",
  fieldPlaceholder: "speed",
  value: "Value",
  valueHelp: "Value stored in this record.",
  kind: "kind",
  checkingField: "Checking this field…",
  more: "More",
  kinds: Object.freeze(["text", "number", "yes/no", "list", "other"]),
  cannotCheck: "can't be checked by a description",
  remove: "Remove",
  notDescribed: "Not described yet",
  notDescribedHelp: "Nothing describes this field. Add it to the fields, open them, or remove it.",
  addToFields: "Add it to the fields",
  openFields: "Open the fields",
  holdsNoKind: "holds data no kind can carry",
  required: "required",
  requiredError: "This field is required.",
  gridSize: (rows, columns) => `${rows} rows, ${columns} columns`,
  gridRagged: (index, columns, otherColumns) => `Row ${index} has ${columns} columns; the others have ${otherColumns}.`,
  gridMismatch: (rows, columns) => `The other grid here is ${rows} by ${columns}. These need to match.`,
  gridEmpty: "A grid needs at least one row.",
  doesNotApply: (field, values) => `Doesn't apply — ${field} is not one of ${values}.`,
  doesNotApplyRemoved: (field, values) => `Doesn't apply — ${field} is not one of ${values}. Its value was removed with this change.`,
  notChecked: "Not checked",
  notCheckedHelp: "Fields whose name starts with an underscore are left alone.",
  unreadable: name => `\`${name}\` can't be read as a record. Open it as text to fix it.`,
  cannotPatch: (name, line) => `\`${name}\` has a repeated key on line ${line}. Open it as text to fix it.`,
  linkNone: "none",
  linkCreate: collection => `Add a record to \`${collection}\`…`,
  add: "Add",
  addLine: "Add a line",
  nestedTooDeep: "This field nests deeper than the form can show. Open it as text.",
  mirroredBy: (field, collection) => `mirrored by \`${field}\` on \`${collection}\``,
  linkDangling: (id, collection) => `\`${id}\` is not a record in \`${collection}\`.`,
  linkRefused: reason => `This link can't be written without leaving the package invalid: ${reason}`,
  removeLink: id => `Remove ${id}`,
  kebabHelp: "must be a lowercase-hyphen name",
  describeFields: "Describe the fields",
  undo: Object.freeze({
    changeField: "Change a field",
    addField: "Add a field",
    removeField: "Remove a field",
    duplicateRecord: "Duplicate a record",
    changeFields: "Change the fields",
    addLine: "Add a line",
    removeLine: "Remove a line",
    changeLink: "Change a link"
  })
});
