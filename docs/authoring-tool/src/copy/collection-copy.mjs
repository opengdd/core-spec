export const counted = (count, singular, plural = `${singular}s`) => `${count} ${count === 1 ? singular : plural}`;

export const COLLECTION_COPY = Object.freeze({
  title: "Collection",
  addRecord: "Add a record",
  empty: "No records yet",
  addFirst: "Add the first one",
  find: "Find",
  duplicate: "Duplicate",
  remove: "Remove",
  recordsCount: count => `${count} ${count === 1 ? "record" : "records"}`,
  otherFields: "other fields",
  notDescribed: "Not described",
  notDescribedHelp: "Records here can hold any fields. Say what a record holds and every record will be checked against it.",
  describeFields: "Describe the fields",
  describeIntro: "These are the fields your records use. Check what each one holds.",
  describeBlocked: field => `\`${field}\` holds data no kind can carry. Rename it \`_${field}\` to keep it as a note nothing checks, or leave this collection free-form.`,
  renameBlocked: field => `Rename it \`_${field}\``,
  matchCount: (count, total) => `${count} of ${total} ${total === 1 ? "record" : "records"} ${total === 1 ? "matches" : "match"} this`,
  describeThem: "Describe them",
  cancel: "Cancel",
  fields: "Fields",
  addField: "Add a field",
  editAsText: "Edit as text",
  removeDescriptions: "Remove the field descriptions",
  removeDescriptionsQuestion: "This deletes the fields you described here, including their sentences, and records stop being checked. Continue?",
  kinds: Object.freeze({
    string: Object.freeze({ label: "text", help: "words, a sentence, a paragraph" }),
    integer: Object.freeze({ label: "whole number", help: "whole numbers, no decimals — 0, 12, -3" }),
    number: Object.freeze({ label: "number", help: "any number, decimals allowed" }),
    grid: Object.freeze({ label: "grid", help: "a board drawn in characters, one line per row" }),
    link: Object.freeze({ label: "link", help: "points to a record in another collection (or this one)" }),
    list: Object.freeze({ label: "lines", help: "a list of small records, each with the same fields" })
  }),
  columns: Object.freeze({
    required: "required",
    unique: "no two records share it",
    choices: "choices",
    pattern: "must be a lowercase-hyphen name",
    when: "only when",
    sentence: "sentence"
  }),
  noSentence: "no sentence yet",
  choicesHelp: "One per line. Lowercase words joined with hyphens, at most 64 characters.",
  exclusive: "A field is either always required or required only when — not both.",
  whenSentence: (field, values) => `only when ${field} is one of ${values}`,
  pointsTo: "points to",
  one: "one",
  many: "many",
  noLoops: "no loops",
  mirroredBy: "mirrored by",
  mirrorEmpty: collection => `no field on \`${collection}\` points back yet`,
  eachLineHolds: "each line holds",
  nestedTooDeep: "This field nests deeper than the form can show. Open it as text.",
  manyRecords: count => `${count} ${count === 1 ? "record" : "records"}`,
  linesCount: count => `${count} ${count === 1 ? "line" : "lines"}`,
  gridSize: (rows, columns) => `${rows} × ${columns}`,
  pastedNames: (count, names) => `${count} ${count === 1 ? "record" : "records"} will be added. Names: ${names}`,
  droppedColumn: column => `\`${column}\` matches no field and will be dropped`,
  unfilledRequired: (field, count) => `\`${field}\` is required and will be empty in ${count} ${count === 1 ? "record" : "records"}`,
  unpastedLines: field => `\`${field}\` holds lines and can't be pasted; those records will have none`,
  pasteRefusal: "Nothing pasted looks like rows. Copy cells from a spreadsheet and try again.",
  gridPasteHelp: "A grid cell: rows separated by \\n",
  readOnlyCell: "read-only — this table cannot edit this cell",
  addThem: "Add them",
  removeMirrorQuestion: (field, collection) => `\`${field}\` on \`${collection}\` says it is mirrored by this field. Remove it there too?`,
  removeBoth: "Remove both",
  keepBoth: "Keep both",
  renameField: (oldName, newName, records, schemas = 0) => `Rename \`${oldName}\` to \`${newName}\` in ${counted(records, "record")}${schemas ? ` and ${counted(schemas, "schema")} that name it` : ""}?`,
  renameRecord: (oldName, newName, mentions, links) => `Rename \`${oldName}\` to \`${newName}\`? ${counted(mentions, "mention")} in your chapters and ${counted(links, "link")} in records will be updated too.`,
  renameCollection: (oldName, newName, records, mentions, fields) => `Rename \`${oldName}\` to \`${newName}\`? This moves ${counted(records, "record")}, updates ${counted(mentions, "mention")} in your chapters, and ${counted(fields, "field")} that ${fields === 1 ? "points" : "point"} here.`,
  removeRecord: (name, mentions, links) => `Remove \`${name}\`? ${counted(mentions, "mention")} in your chapters and ${counted(links, "link")} in records will be left pointing at nothing.`,
  removeCollection: (name, records) => `Remove \`${name}\` and its ${counted(records, "record")}?`,
  bareMentions: (count, name) => `${counted(count, "other place")} in your files ${count === 1 ? "says" : "say"} \`${name}\` as a word; ${count === 1 ? "that is" : "those are"} left as ${count === 1 ? "it is" : "they are"}.`,
  renameQuestion: "What should it be called now?",
  renamePreview: name => `Saved as \`${name}\``,
  rename: "Rename",
  renameKebab: "Use lowercase words joined with hyphens.",
  renameFieldName: "Use lowercase letters, digits, underscores, or internal hyphens.",
  renameTaken: name => `\`${name}\` is already taken. Pick another name.`,
  renameUnknown: name => `\`${name}\` is not a name in this package.`,
  renameChanged: "The package changed since this was planned; look again.",
  recordMoveRefused: "A record lives in its collection; rename it or remove it.",
  collectionMoveRefused: "A collection lives under collections/; rename it or remove it.",
  validationRefused: reason => `This change can't be written without leaving the package invalid: ${reason}`,
  undo: Object.freeze({
    duplicateRecord: "Duplicate a record",
    pasteRecords: "Paste records",
    changeField: "Change a field",
    changeFields: "Change the fields",
    addField: "Add a field",
    removeField: "Remove a field",
    renameField: "Rename a field",
    describeFields: "Describe the fields",
    removeDescriptions: "Remove the field descriptions",
    removeRecord: "Remove a record",
    renameRecord: "Rename a record",
    renameCollection: "Rename a collection",
    removeCollection: "Remove a collection"
  })
});
